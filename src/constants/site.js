/** Canonical site origin (production). */
export const SITE_URL = "https://sendquote.ai";

export const SITE_NAME = "SendQuote";

/** Default Open Graph / social image (absolute URL). */
export const DEFAULT_OG_IMAGE_URL = `${SITE_URL}/images/landingImg.webp`;
